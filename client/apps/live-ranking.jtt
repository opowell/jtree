app.title='live ranking';
app.groupSize= 2;


var stage1 = app.newStage('stage1');
stage1.playerStart = function(player){
  player.x = Math.random();
}
stage1.updateObject='group';
stage1.activeScreen = `
      {{group.players[0].x}}
      {{group.players[1].x}}
      <form>
          <div id='tasksDiv'></div>
          <button>OK</button>
      </form>
`;
